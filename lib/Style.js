'use strict'

var React = require('react-native')
var {
  StyleSheet
} = React

var style = StyleSheet.create({
  IndicatorWrap: {
    justifyContent: 'center',
    alignItems: 'center',
    left: 0,
    right: 0
  }
})

module.exports = style
